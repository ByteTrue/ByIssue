# 姿态路由场景表

`bi` 的行为链是「读用户这句话 → 选一个姿态 → 只读那个姿态的必读 → 按那套规则做事」。选错姿态，后面全错。这张表是那条链的期望值。

## 怎么跑

**主跑法：直接用 subagent，不要写脚本。** 执行这份表的是 agent，不是 shell——手上有 subagent 工具就直接用它，别把自己降级成只会调 `python3` 的执行器。

每条场景起一个子 agent，并行开：

- **`cwd` 指向一份可写的临时副本**：`rsync -a --exclude .git ./ /tmp/bi-eval/`。子 agent 必须能真的动手，否则行为会变形——首次评测用只读工具，结果第一条直接回「我改不了文件」就收工，那条数据作废。
- **给完整工具，不要限制为只读。** 副本提供隔离，工具限制只会污染观测。
- **`maxTurns` 至少 15。** 首次用 8，三条被截断在关键处。
- `task` **只放那一句话**，不要附加「请说明你进入了哪个姿态」——那会污染路由。
- 跑完读子会话日志，看它**实际读了哪些文件**，与本表比对。判据是调用序列，不是它自己的说法。

**注意技能来源：** 子 agent 加载的是部署副本 `~/.agents/skills/bi/`，不是仓库里的 `skills/bi/`。改完必须先 rsync 同步再跑，否则测的是旧版本。

为什么必须是子 agent 而不是你自己：上下文隔离。已经参与过设计讨论的 agent 知道答案，测自己等于自己判自己。

**「不应读取」比「应读取」更能暴露问题**：多读一个文件意味着路由没收敛，正是这个技能要避免的。

**结构体检（附带）：** `python3 tools/check-skill-repository.py` 会校验姿态表没坏、触发词不被两个姿态同时占用、每个姿态都有场景覆盖。它**不验证语义路由**，只是棘轮——新增姿态时不补场景就红。

改过 `SKILL.md` 姿态表或任一必读文件后，两个都跑一遍。

## 场景

| # | 说法 | 期望姿态 | 应读取 | 不应读取 |
|---|---|---|---|---|
| 1 | 先聊清楚这个改动再动手 | 讨论 | talk | do、fast、design |
| 2 | 帮我规划一下这块怎么做 | 讨论 | talk | do、design |
| 3 | 这条链路是怎么工作的 | 理解现状 | explore | design、do |
| 4 | 影响范围有多大 | 理解现状 | explore | do |
| 5 | 快速修一下这个文案 | 快交付 | fast | do、design、complain |
| 6 | 小改一下，别走流程 | 快交付 | fast | do、design |
| 7 | 推进这个 issue | 受管理实现 | do | fast、talk |
| 8 | 先打通这条主路径再加厚 | 受管理实现 | do | explore |
| 9 | 这里坏了，和预期不一样 | 修 bug | complain | design、do |
| 10 | 这个 bug 我 debug 半天没头绪 | 修 bug | complain、debug | design |
| 11 | 先设计一下实现方案 | 设计 | design | do、fast |
| 12 | 关闭并沉淀 | 收尾 | close | do、design |
| 13 | 看看这个 diff 有没有问题 | 审代码 | code-design | do、fast |
| 14 | 记一下这个坑 | 记知识 | note | close、spec |
| 15 | 维护一下 spec，这块真相变了 | 规格 | spec | do、design |
| 16 | 整理一下 vision，说说应用将来什么样 | 愿景 | vision | spec、do |
| 17 | 在这个项目接入 ByIssue | 接入 | onboard | talk、spec |
| 18 | 我带你跑一遍这个发布流程 | 学流程 | maketools | do、fast |

## 边界场景（更容易翻车的那些）

| # | 说法 | 期望姿态 | 关键断言 |
|---|---|---|---|
| 19 | 加个字段就行，很简单 | 快交付 | 不建常规 issue；完成后落 `ff` |
| 20 | 做这个 issue，但先别留痕迹 | 受管理实现 | 仍跑开工协议与验证；不写 `ff` |
| 21 | 这个能力要跨三个模块重做 | 讨论 或 受管理实现 | 不默认走快交付；升管理强度前先说明理由 |
| 22 | 顺手把这个也改了吧 | 快交付 | 明确本次范围；范围翻倍时提出跳出快改 |
| 23 | 继续 talk（在一次被授权的落盘动作之后） | 讨论 | **不提施工方案、不落盘**；动作级授权不翻转姿态 |
| 24 | review 一下整个仓库 | 审代码 | 先要范围；不做无边界全仓审计 |

场景 23 来自真实翻车，见 `byissue/issues/005-o-posture-stability-across-authorized-actions.md`。
